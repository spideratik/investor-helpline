info:
  name: Untitled
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/auth/signup
  body:
    type: json
    data: |-
      {
        "username": "Admin",
        "email": "admin@protiti.com",
        "password": "password",
        "role": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
